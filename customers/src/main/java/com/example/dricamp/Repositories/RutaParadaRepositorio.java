package com.example.dricamp.Repositories;

import com.example.dricamp.models.RutaParada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RutaParadaRepositorio extends JpaRepository<RutaParada, Long> {
}
